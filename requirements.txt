fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
aiosqlite>=0.20.0
httpx>=0.28.0
pydantic>=2.10.0
yt-dlp>=2025.1.0
pdfplumber>=0.11.0
ebooklib>=0.18
jinja2>=3.1.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Claude Agent SDK — installed from the vendored submodule so the pinned
# source is the single source of truth (see .gitmodules). The `claude` CLI it
# drives is provided separately: on PATH locally, and via npm in the image.
./claude-agent-sdk-python
